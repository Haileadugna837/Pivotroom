# reviews

Status: not built

Post-session ratings and reviews left by clients for experts.

- `components/` — review form, review list on expert profile
- `server/` — CRUD for reviews, average rating aggregation

Depends on: Supabase schema (`reviews` table), `booking` completion state.
