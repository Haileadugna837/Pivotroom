# experts

Status: not built

Expert profiles, discovery/search, categories, pricing per session.

- `components/` — profile page, expert card, search/filter UI
- `server/` — queries against `experts` table, profile CRUD

Depends on: Supabase schema (`experts`, `categories` tables) — not yet created.
