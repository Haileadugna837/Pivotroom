# auth

Status: not built

Login/signup for clients, experts, and admin. Email/password and Google OAuth via Supabase Auth.

- `components/` — login form, signup form, auth guards
- `server/` — role helpers (client/expert/admin), session utilities

Depends on: Supabase Auth Google provider (dashboard config, pending Google Client ID/Secret).
